#pragma once
#ifndef GEODESY_CORE_MATH_NUMBER_TYPE_H
#define GEODESY_CORE_MATH_NUMBER_TYPE_H

typedef unsigned char uchar;
typedef unsigned short ushort;
typedef unsigned int uint;
//typedef unsigned long ulong;

//typedef char byte;

//typedef ushort half;
//typedef float;
//typedef double;

/*
namespace geodesy::core::math {

	// Apparently windows has their own definitions. 
#ifndef _WINDOWS_
	template <typename T>
	T min(T aA, T aB);
	template <typename T>
	T max(T aA, T aB);
#endif

	template <typename T>
	T ln(T Arg);

	template <typename T>
	T log(T Base, T RetVal);
	//T atan(T ArgY, T ArgX);

	template <typename T>
	T deg2rad(T Arg);

	template <typename T>
	T rad2deg(T Arg);

}
*/

#endif // !GEODESY_CORE_MATH_NUMBER_TYPE_H
